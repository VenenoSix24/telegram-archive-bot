# secrets & credentials
.env
.env.*
!.env.example

# 真实 config (only config.example.yaml is committed)
config.yaml
config.yaml.bak
config.yaml.*.bak

# telethon session (禁止提交 GitHub)
*.session
*.session-journal

# database
*.db
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal
*.sqlite.*.bak

# logs
logs/

# python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/

# macOS
.DS_Store

# 缩略图本地缓存（web V2 浏览用，不进 Git）
thumbs/

# web 前端构建与依赖
web/node_modules/
web/dist/

# 本地设计文档与决策记录（开发规范 3.4：不进公开仓库）
开发文档*.md
开发规范*.md
最新对接文档.md
任务清单.md

# 前端设计 demo（本地审阅用，不进 Git）
design_demo/
# superpowers 工作文档整体不进 Git（含 specs 等）
docs/
.history/

# Claude Code 本地运行配置（机器相关，不进 Git）
.claude/launch.json

# changelog (maintained outside repository for now)
CHANGELOG.md

# 多目标独立编辑实现计划

## 目标
让每个归档目标副本独立保存和修改正文、HTML、Tag、Rating、渲染结果，并从 Web 详情抽屉选择目标、编辑正文，兼容旧单目标数据。

## 阶段
- [x] 设计并提交独立目标编辑规范
- [ ] 完成目标副本存储与编辑服务
- [ ] 完成 API 返回和 PATCH 目标选择
- [ ] 完成详情抽屉目标选择与正文编辑
- [ ] 补充测试并运行全量检查

## 关键约束
- 不实现目标删除监听。
- 不改变旧 Telegram 命令调用路径。
- 不带 target_id 的旧 API 请求继续编辑主 messages 记录。
- target_id 必须属于当前 message_id；无效时返回 404。
- Telegram 编辑成功后才提交目标副本内容。

## 遇到的错误
暂无。
